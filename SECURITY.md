# 安全政策

## 支持范围

本仓库是课程项目和研究原型，不提供生产级安全支持。维护者会优先处理影响数据安全、依赖供应链、任意代码执行、路径遍历、凭据泄露和 WebUI 暴露风险的问题。

| 版本 | 支持状态 |
|---|---|
| `main` | 接受安全修复 |
| 历史分支或本地实验分支 | 不承诺维护 |

## 报告安全问题

请不要在公开 issue 中披露可被直接利用的漏洞细节、敏感数据、凭据或私有数据链接。

推荐报告方式：

1. 优先使用 GitHub Security Advisories 的私密报告功能。
2. 如果仓库尚未启用私密报告，请联系课程项目负责人或仓库维护者。
3. 报告中请包含影响范围、复现步骤、相关 commit、预期风险和建议修复方向。

## 数据与隐私

- 不要提交原始 NIfTI 数据、训练权重、预测结果、日志中的本地敏感路径或访问凭据。
- 共享医学影像数据前必须确认数据来源、授权范围和脱敏状态。
- 如果发现疑似敏感数据已经进入 Git 历史，请立即联系维护者，不要只用普通 commit 删除文件。

## 依赖安全

新增依赖必须写入 `pyproject.toml` 并通过 `uv` 同步。提交 PR 前请尽量使用最小必要依赖，避免引入无人维护或来源不明的包。
