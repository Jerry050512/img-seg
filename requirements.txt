# DEPRECATED compatibility file for the course assistant's pip workflow.
# The authoritative dependency declarations are pyproject.toml and uv.lock.
# Prefer: uv sync
--extra-index-url https://download.pytorch.org/whl/cu121

albumentations>=2.0.0
gradio>=5.0.0
matplotlib>=3.9.0
monai>=1.4.0
nibabel>=5.3.0
nnunetv2>=2.5.0
numpy>=2.0.0
opencv-python>=4.10.0
pandas>=2.2.0
pyyaml>=6.0.0
scikit-learn>=1.5.0
scipy>=1.14.0
segmentation-models-pytorch>=0.4.0
timm>=1.0.0
torch==2.5.1+cu121; platform_system != "Darwin"
torch==2.5.1; platform_system == "Darwin"
torchvision==0.20.1+cu121; platform_system != "Darwin"
torchvision==0.20.1; platform_system == "Darwin"
tqdm>=4.66.0
